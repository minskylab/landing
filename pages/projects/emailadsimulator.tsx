import React, { FC } from "react";
import { NextPage } from "next";

const SimulatorPage: FC<NextPage> = (props: NextPage) => {
    return <div>Simulator Page</div>;
};

export default SimulatorPage;
